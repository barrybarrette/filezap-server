language: python
python:
  - "3.6"
install:
  - pip install -r requirements.txt
jobs:
  include:
    - stage: unittest
      script: bolt cov
    - stage: deploy
      if: branch = master AND type = push
      script: zappa update dev
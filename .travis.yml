language: python
python:
  - "3.6"
install:
  - pip install -r requirements.txt
jobs:
  include:
    - stage: unittest
      script: bolt cov
    - stage: deploy
      if: branch = master
      script: zappa update dev